{
  "name": "app-governance",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "agent-guard": "bin/agent-guard"
  },
  "scripts": {
    "generate": "node --import tsx tools/generate-adapters/index.ts",
    "agent-guard": "node --import tsx tools/agent-guard/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test test/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.20.0",
    "typescript": "^5.9.0",
    "yaml": "^2.8.0"
  }
}
